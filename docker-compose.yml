version: "3.7"
services:
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile.dev
    ports:
      - "4200:4200"
      - "35729:35729"
    volumes:
      - .:/app
      - node-modules:/frontend/node_modules
volumes:
  node-modules:
