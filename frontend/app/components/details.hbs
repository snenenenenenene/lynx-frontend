{{page-title 'Info'}}
{{!-- <div style='width: 100%; display: flex; justify-content: end; gap: 10px;'>
  <AuButton @icon='chevron-left' {{on 'click' this.nextGraph}} />
  <AuButton @icon='chevron-right' {{on 'click' this.nextGraph}} />
</div> --}}
<div style='display:flex;'>

  <div style='width: 33%;'>
    <C3Chart
      @data={{this.graphData1}}
      @title={{this.graphTitle1}}
      @color={{this.color}}
      @padding={{this.padding}}
    />
  </div>
  <div style='width: 33%;'>
    <C3Chart
      @data={{this.graphData2}}
      @title={{this.graphTitle2}}
      @padding={{this.padding}}
      @color={{this.color}}
    />
  </div>
  <div style='width: 33%;'>
    <C3Chart
      @data={{this.graphData3}}
      @title={{this.graphTitle3}}
      @padding={{this.padding}}
      @color={{this.color}}
    />
  </div>
  <h1>{{this.municipality.averageRevenue}}</h1>
  <h1>{{this.municipality.totalRevenue}}</h1>
</div>
